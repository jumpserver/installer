on: [push, pull_request, release]

name: JumpServer repos generic handler

jobs:
  generic_handler:
    name: Run generic handler
    runs-on: ubuntu-latest
    steps:
      # Pinned to the reviewed master revision; update deliberately.
      - uses: jumpserver/action-generic-handler@35e782f25ac2f2d15e7ab7ec07b0ae6461138b0b
        env:
          GITHUB_TOKEN: ${{ secrets.PRIVATE_TOKEN }}
