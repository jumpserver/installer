services:
  panda:
    image: ${NAMESPACE:-jumpserver}/panda:${VERSION}
    container_name: jms_panda
    hostname: jms_panda
    ulimits:
      core: 0
    restart: always
    env_file:
      - ${CONFIG_FILE}
    volumes:
      - ${VOLUME_DIR}/panda/data:/opt/panda/data
      - /var/run/docker.sock:/var/run/docker.sock:z
    healthcheck:
      test: "check http://localhost:9001/panda/health/"
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 10s
    networks:
      - net
